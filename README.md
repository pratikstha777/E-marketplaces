# E-Marketplace Full Stack
A decoupled e-commerce platform built with Django REST Framework and React (Vite).

## 🚀 Tech Stack
- **Frontend:** React, React Router, JWT Authentication.
- **Backend:** Django, Postgres, Redis (Dockerized).
- **Deployment:** Vercel (Frontend/Backend), Neon (Database).

## 🛠️ Getting Started
1. Clone the repo: `git clone ...`
2. Run Backend: `pip install -r requirements.txt && python manage.py runserver`
3. Run Frontend: `cd frontend && npm install && npm run dev`

## 🧪 Testing
- Backend: `python manage.py test`
- Frontend: `npm test`